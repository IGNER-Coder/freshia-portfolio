export const metadata = {
  title: 'Contact',
  description: 'Get in touch with Freshia Njeri for commissions, exhibitions, or studio visits through the contact form or direct email.',
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}